# DO NOT USE FOR RHEMA CARE FLOW — LEGACY LOVABLE PROJECT

This repository is not the canonical Rhema Care Flow project.

Do not use this repository for Lovable, Vercel, Supabase, GitHub Actions, or production deployment of Rhema Care Flow.

Canonical repository:

```text
JoaoRG-lab/rhema-care-flow
```

Canonical URL:

```text
https://github.com/JoaoRG-lab/rhema-care-flow
```

Canonical branch:

```text
main
```

Correct deployment settings:

```text
Framework: Vite
Install command: npm install
Build command: npm run build
Output directory: dist
```

If any automation tool is using `joaooz123-png/vida-clinica-os-bc012bad` for Rhema Care Flow, it is misconfigured and must be reconnected to `JoaoRG-lab/rhema-care-flow`.
